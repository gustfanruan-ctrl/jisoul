# backend/requirements.txt
# JiSoul MVP 依赖列表
# 变更：新增 Cross-Encoder reranker 依赖

# ============ Web 框架 ============
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ============ 向量数据库 ============
chromadb>=0.5.0

# ============ Embedding & Reranking ============
sentence-transformers>=3.0.0  # 支持 embedding + reranker
torch>=2.6.0

# ============ LLM 调用 ============
openai>=1.0.0  # OpenAI 兼容协议
httpx>=0.27.0

# ============ 数据处理 ============
pydantic>=2.10.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ============ 文档处理 ============
python-docx>=1.0.0  # DOCX 解析

# ============ 日志 ============
loguru>=0.7.0

# ============ 其他 ============
numpy>=1.26.0